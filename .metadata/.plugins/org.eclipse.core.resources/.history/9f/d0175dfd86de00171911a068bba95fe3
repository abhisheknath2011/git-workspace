/**
 * 
 */
package com.abhishek.code;

/**
 * @author Abhishek Nath
 *
 */
public class RemoveAdjacentDuplicates {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		String input = "abbbcccaddbceea";
		String output = removeAdjDuplicates(input);
		System.out.println("output : "+output);
	}
	
	public static String removeAdjDuplicates(String input) {
		StringBuilder builder = new StringBuilder();
		int index = 0;
		char ch;
		boolean skip=false;
		while(index < input.length()) {
			ch = input.charAt(index++);
			builder.append(ch);
			while(input.charAt(index++) == builder.charAt(builder.length()-1)) {
				skip=true;
			}
			if(skip) {
				builder.deleteCharAt(builder.length()-1);
				skip = false;
			}
		}
		return builder.toString();
	}
	
	public static void remove(String input, StringBuilder builder) {
		if(input.isEmpty()) {
			return;
		}
		char ch = input.charAt(0);
		if(builder.length()==0) {
			builder.append(ch);
		}else {
			if(ch == builder.charAt(builder.length()-1)) {
				
			}
		}
	}

}
