/**
 * 
 */
package com.abhishek.code;

import java.util.HashSet;
import java.util.Set;

/**
 * @author Abhishek Nath
 *
 */
public class DistinctSubstrings {

	/**
	 * @param args
	 */
	
	public static void main(String[] args) {
		Set<String> substrSet = new HashSet<String>();
		findDistinctSubstrings("ababa", 1);

	}
	
	private static void findDistinctSubstrings(String str, int j) {
		if(j==str.length()) {
			System.out.println(str);
			return;
		}
		for(int i=0;i<=str.length()-j;i++) {
			String substr = str.substring(i, i+j);
			System.out.println(substr);
		}
		findDistinctSubstrings(str, j+1);
	}

}
